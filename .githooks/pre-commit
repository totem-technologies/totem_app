#!/usr/bin/env sh

# Pre-commit hook to check dart formatting
# To use this hook, run: make githooks

set -u

# Match Makefile: a PATH `dart` is often 3.9 and cannot compile this
# workspace (sdk: ^3.11.3). FVM uses the SDK from .fvmrc.
fvm dart scripts/check_format.dart
