# development flavor — local defaults. Override individual keys for your own
# machine in config/development.local.env (gitignored); see config/README.md.
ENVIRONMENT=development
API_URL=https://totem.local
