name: totem_web
description: "Guided introspection groups."
publish_to: "none"
version: 0.0.59+59
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  totem_core:
    path: ../totem_core

  # TODO(web): Remove dependency on totem_app
  totem_app:
    path: ../totem_app

  # Firebase (used by generated firebase_options.dart)
  firebase_core: ^4.0.0

  # State Management
  flutter_riverpod: ^3.2.1

  go_router: ^17.2.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
